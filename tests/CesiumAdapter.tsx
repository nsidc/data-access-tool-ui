import * as React from "react";
import { shallow } from "enzyme";
const Cesium = require("cesium/Cesium");

import CesiumAdapter from "../src/components/CesiumAdapter";

test("Creates Cesium viewer", () => {
  console.log(Cesium);
});
