const Cesium = require("cesium/Cesium");

describe("Cesium adapter", () => {
  test("Creates Cesium viewer", () => {
      // The only current purpose of this test is to demonstrate mocking.
      // Uncomment the "console" 
    console.log(Cesium);
  });
});
