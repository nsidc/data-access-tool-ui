import * as React from "react";

// let DrawHelper = require("./DrawHelper");
import "./DrawHelper.css";

export class Toolbar extends React.Component {
    render() {
      return (
        <div id="toolbar"><p>Toolbar goes here!</p></div>
      );
    }
}
